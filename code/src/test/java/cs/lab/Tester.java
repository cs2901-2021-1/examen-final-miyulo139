package cs.lab;

import org.testng.Assert;
import org.testng.annotations.Test;

class Tester {
    /*@Test
    public void testlogin() {
        var gen = General.getUniqueInstance();
        gen.login("milagros", "sorgalim");
        Assert.assertNotEquals(gen.getUsuariosActivos().get("milagros"), null);
    }


    @Test
    public void testDardeAlta() {
        var gen = General.getUniqueInstance();
        gen.login("milagros", "sorgalim");
        gen.addCentrosdeVacunacion("Solar");
        gen.darDeAlta("milagros","Solar");
        var boo = gen.getCentrosDeVacunacion().get("Solar").isDeAlta();
        Assert.assertEquals(boo, true);
    }

    @Test
    public void testDardeBaja() {
        var gen = General.getUniqueInstance();
        gen.login("milagros", "sorgalim");
        gen.addCentrosdeVacunacion("Solar");
        gen.darDeBaja("milagros","Solar");
        var boo = gen.getCentrosDeVacunacion().get("Solar").isDeAlta();
        Assert.assertEquals(boo, false);
    }

    */
}